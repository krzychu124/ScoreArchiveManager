package pl.applicationserver.scorefilesmanager.domain;

public class ArchivedFileMetadata extends AbstractFileMetadataArchive {

    public ArchivedFileMetadata(AbstractFileMetadata fileMetadata) {
        super(fileMetadata);
    }
}
